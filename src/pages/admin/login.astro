---
import Layout from "@/layouts/Layout.astro";

let errorMessage: string | null = null;

// Check for error in query params (from failed API login)
const url = new URL(Astro.request.url);
if (url.searchParams.get('error') === 'invalid') {
  errorMessage = "Invalid username or password";
} else if (url.searchParams.get('error') === 'inactive') {
  errorMessage = "Account is deactivated";
} else if (url.searchParams.get('error') === 'missing') {
  errorMessage = "Username and password are required";
}
---

<Layout title="Admin Login" isLandingPage={false}>
  <div class="flex min-h-screen items-center justify-center">
    <div class="w-full max-w-sm p-6 bg-gray-800 rounded-lg shadow-md">
      <h2 class="text-2xl font-bold text-white text-center mb-6">Admin Login</h2>
      
      {errorMessage && (
        <div class="bg-red-500/10 border border-red-500 text-red-500 px-4 py-2 rounded mb-4">
          {errorMessage}
        </div>
      )}

      <form method="POST" action="/api/login" id="loginForm">
        <div class="space-y-4">
          <div>
            <label for="username" class="block text-sm font-medium text-gray-300">Username</label>
            <input
              type="text"
              name="username"
              id="username"
              value="admin"
              autocomplete="username"
              required
              class="mt-1 block w-full px-3 py-2 bg-gray-700 border border-gray-600 rounded-md text-white shadow-sm focus:border-indigo-500 focus:ring focus:ring-indigo-500/50"
            />
          </div>

          <div>
            <label for="password" class="block text-sm font-medium text-gray-300">Password</label>
            <input
              type="password"
              name="password"
              id="password"
              autocomplete="current-password"
              required
              class="mt-1 block w-full px-3 py-2 bg-gray-700 border border-gray-600 rounded-md text-white shadow-sm focus:border-indigo-500 focus:ring focus:ring-indigo-500/50"
            />
          </div>

          <button
            type="submit"
            id="submitBtn"
            class="w-full py-2 px-4 bg-indigo-600 hover:bg-indigo-700 text-white font-medium rounded-md focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
          >
            Sign in
          </button>
        </div>
      </form>
    </div>
  </div>
</Layout>

<script>
  // Debug form submission
  const form = document.getElementById('loginForm');
  const submitBtn = document.getElementById('submitBtn');
  
  if (form && submitBtn) {
    console.log('🔍 Login form found, adding debug listeners');
    
    form.addEventListener('submit', function(e) {
      console.log('🚀 Form submit triggered');
      console.log('Form action:', form.action);
      console.log('Form method:', form.method);
      
      const formData = new FormData(form);
      console.log('Form data:', {
        username: formData.get('username'),
        password: formData.get('password') ? 'PROVIDED' : 'MISSING'
      });
    });
    
    submitBtn.addEventListener('click', function(e) {
      console.log('🖱️ Submit button clicked');
    });
  } else {
    console.error('❌ Could not find login form or submit button');
  }
</script>
